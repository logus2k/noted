# Skill: mlflow-snapshots

**Type:** skill
**Source:** [data/skills/mlflow-snapshots/SKILL.md](../../../data/skills/mlflow-snapshots/SKILL.md)

## Purpose

MLflow snapshots (mid-training Logged Models).

## Scenarios

### S1 - What is a snapshot
Mid-training Logged Model; noted captures automatically.

### S2 - Snapshots per run
`get_run_details`; Logged Models count.

### S3 - Register a snapshot
`register_model` with specific `artifact_path`.

### S4 - Snapshot vs final
Deploy final; snapshots for analysis.

### S5 - Retention (DEFERRED)
### S6 - Per-epoch checkpointing (DEFERRED)
