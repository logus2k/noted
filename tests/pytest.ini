[pytest]
testpaths = api e2e
markers =
    api: Backend API tests
    e2e: Playwright end-to-end tests
    socketio: Socket.IO kernel/terminal tests
    slow: Tests that take >30 seconds (venv creation, training)
timeout = 120
asyncio_mode = auto
